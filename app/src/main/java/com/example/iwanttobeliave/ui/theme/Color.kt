package com.example.iwanttobelieve.ui.theme  // Pacote do tema

import androidx.compose.ui.graphics.Color      // Importa a classe Color do Compose

// Paleta básica do app (pode ajustar depois)
// Cor primária (botões/acentos)
val Primary = Color(0xFF2E7D32)   // Verde
// Variações
val PrimaryDark = Color(0xFF1B5E20)  // Verde escuro
val PrimaryLight = Color(0xFF60AD5E) // Verde claro

// Cores auxiliares
val Secondary = Color(0xFF2979FF)    // Azul secundário
val Background = Color(0xFFF6F6F6)   // Fundo claro
val Surface = Color(0xFFFFFFFF)      // Cards/superfícies
val Error = Color(0xFFB00020)        // Erro padrão Material
